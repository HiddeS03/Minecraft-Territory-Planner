services:
  backend:
    build:
      context: ./backend
    ports:
      - "3001:3001"
    volumes:
      - territory_data:/app/data

  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  territory_data:
